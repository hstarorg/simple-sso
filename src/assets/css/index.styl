.page-index {
  .ui.card {
    width: auto;
  }
  .user-card {
    width: 500px;
    margin: 0 auto;
    border-radius: 10px;
    box-shadow: 0 1px 2px 0 rgba(34, 36, 38, 0.15);

    .user-avatar img {
      width: 50%;
      margin-left: 25%;
      border-radius: 50%;
    }
  }
}
